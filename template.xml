<?xml version="1.0" encoding="utf-8"?>
<Peach xmlns="http://phed.org/2008/Peach" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
	xsi:schemaLocation="http://phed.org/2008/Peach /peach/peach.xsd">

	<!-- Import defaults for Peach instance -->
	<Include ns="default" src="file:defaults.xml"/>

	<!-- TODO: Create data model -->
	<DataModel name="TheDataModel">
		<Blob lengthType="calc" length="" />
	</DataModel>

	<!-- TODO: Create state model -->
	<StateModel name="TheState" initialState="Initial">

		<State name="Initial">
			<Action type="output" publisher="File">
				<DataModel ref="TheDataModel"/>
				<Data fileName="sample.png" />
			</Action>
			<Action type="close" publisher="File" />
			<Action type="call" method="asdasd" publisher="DoLaunch" />
		</State>

	</StateModel>

	<Test name="TheTest">
		<StateModel ref="TheState"/>

		<Publisher name="File" class="file.FileWriter">
			<Param name="filename" value="fuzzed.png"/>
		</Publisher>
		<Publisher name="DoLaunch" class="process.Launcher">
			<Param name="Command" value="bin\pngcheck.exe"/>
			<Param name="Args" value="fuzzed.png"/>
		</Publisher>
	</Test>

	<!-- Configure a single run -->
	<Run name="DefaultRun">

		<!-- TODO: Change log path if needed -->
		<Logger class="logger.Filesystem">
			<Param name="path" value="logs"/>
		</Logger>

		<Test ref="TheTest"/>

	</Run>

</Peach>
<!-- end -->
